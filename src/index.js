//import {walkAndLoadEnv, walkAndFlattenLoadEnv} from '@ktr-srt/environment'
//import env from '@ktr-srt/environment'

// walkAndLoadEn



import {walkAndLoad} from './env.js'
import Env from './archive/Env.js'

const envObj = new Env()

// await env.init(path3, path1, 'up', '__ktr-srt__')
// const res = env.flatten()j

// walkAndLoadEnv(startDirPath, endDirPath, direction, prefix)